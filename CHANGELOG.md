# Changelog

Notable changes to `git-why` are recorded here.

The project is not yet published. Until the first release, entries remain under Unreleased.

## Unreleased

### Added

- Evidence-first analysis for a tracked `FILE:LINE` at `HEAD`.
- Human-readable and schema-versioned JSON output.
- Local line history with explicit completeness and dirty-worktree warnings.
- GitHub reference links derived locally from a recognized origin remote.
- Safety limits for source blobs and commit metadata.
